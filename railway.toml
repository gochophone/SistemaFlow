[build]
builder = "RAILPACK"

[deploy]
startCommand = "uvicorn server:app --app-dir backend --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
