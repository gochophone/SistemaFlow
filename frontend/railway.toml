[build]
builder = "RAILPACK"
buildCommand = "CI=false yarn build"

[deploy]
startCommand = "npx --yes serve@14.2.4 -s build -l tcp://0.0.0.0:$PORT"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
